using namespace std;
void permute(string a, int l, int r)
{
     if (l == r)
        cout<<a<<endl;
    else
        for (int i = l; i <= r; i++)
        {
          permute(a, l+1, r);  
    
        }
}
int main()
{
    string str = "ABC";
    int n = str.size();
    return 0;
}
